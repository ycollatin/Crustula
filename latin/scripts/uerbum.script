#! /usr/bin/python3
from crustula.models import *
import csv

with open("scripts/crustula_uerbum.csv") as csvfile:
    reader = csv.DictReader(csvfile)
    for row in reader:
        print(row)
        u = Uerbum(name=row["name"],latine = row["latine"], gallice = row["gallice"])
        u.save()
